apiVersion: v1
kind: Service
metadata:
  name: helloworld-service
  labels:
    app: helloworld
    visualize: "true"
spec:
  type: LoadBalancer
  ports:
  - port: 3001
  selector:
    app: helloworld

    
